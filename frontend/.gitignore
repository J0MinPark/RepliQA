# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.local

# mcp-server/package.json의 build 스크립트가 여기로 복사해준다 — mcp-server 소스가
# 바뀌면 mcp-server에서 `npm run build`를 다시 돌려야 최신 상태로 갱신된다.
public/repliqa-mcp.bundle.mjs

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.env*
