// 실사이트(성균관대 공지사항 게시판) QA에서, 6개 카테고리를 전부 나열한 체크리스트
// 형태가 LLM에게 "카테고리마다 하나씩 채워야 한다"는 압박으로 작용해 정상적인 사이트에도
// 카테고리당 1개씩, 총 8개의 지적(스타일 취향에 가까운 것들 포함)이 나온 걸 실측으로
// 확인했다 — "체크리스트를 다 훑어라"가 아니라 "명백히 망가진 것만 걸러내라"로 프레이밍을
// 바꾼다. 지적하면 안 되는 것의 구체적 예시를 먼저 주는 게, 추상적으로 "애매하면 넣지
// 마라"고 말하는 것보다 실제로 더 잘 지켜졌다(부정 예시가 없던 이전 버전 대비).
const UIUX_CHECKLIST = `
아래 기준은 "명백히 망가진 것"을 찾을 때만 참고하는 참고용 목록이다. 6개 카테고리를 전부
채우려 하지 마라 — 정상적으로 만들어진 사이트는 findings가 0~1개인 게 정상이고, 실제로
그래야 한다. 카테고리마다 뭔가 하나씩 짜내려고 하지 마라.

절대 findings에 넣으면 안 되는 것(이건 "문제"가 아니라 디자이너의 선택이다):
- "더 통일감 있었으면 좋겠다", "여백이 더 일정했으면" 같은 스타일 취향 — 실제로 못 쓰게
  막는 게 아니면 지적하지 마라.
- 폰트 크기가 여러 단계로 쓰이는 것 자체, 아이콘/버튼 디자인이 화면마다 조금씩 다른 것 —
  실무의 거의 모든 사이트가 그렇다. 같은 버튼인데 어떤 화면에선 링크처럼, 어떤 화면에선
  버튼처럼 보여 "이게 눌리는 건지" 헷갈리는 수준이 아니면 지적하지 마라.
- "ABEEK" 같은 그 사이트/업계 자체 용어 — 그 사이트의 실제 사용자층(예: 재학생, 특정
  업계 종사자)이 이미 아는 용어라면 "전문용어라 어렵다"고 지적하지 마라. 너는 그 사이트의
  실제 타겟 사용자가 아니다.
- "조금 더 눈에 띄었으면", "조금 더 크면 좋겠다" 같은 강조 정도의 차이 — 실제로 못
  찾는 수준(예: 버튼이 배경에 파묻혀 존재 자체를 모를 정도)이 아니면 지적하지 마라.
- 요소 크기가 터치하기에 작아 보인다는 지적 — 이 화면은 데스크톱 뷰포트로 렌더링된
  것이라, 마우스로 조작하는 걸 전제로 한다. 모바일 터치 기준을 적용하지 마라(이미
  결정론적 체크가 별도로 처리함).

findings에 넣어야 하는 것(실제로 사용자를 막거나 확실히 헷갈리게 하는 것만):
1. 접근성 — 텍스트가 배경과 겹쳐 정말 읽기 어려운 수준, alt 텍스트가 아예 없는 의미
   있는 이미지, 키보드로 포커스를 옮겼을 때 지금 어디 있는지 전혀 알 수 없는 경우.
2. 일관성 — 똑같은 기능(예: "저장"/"제출" 버튼)이 화면마다 완전히 다르게 생겨서 같은
   기능인지 못 알아보는 수준.
3. 레이아웃 — 요소가 서로 겹쳐서 내용을 가리거나, 화면 밖으로 잘려서 아예 안 보이는 경우,
   불필요한 가로 스크롤.
4. 피드백 — 버튼을 눌러도 로딩 중인지 아무 표시가 없어 멈춘 것처럼 보이는 경우, 에러
   메시지가 원인을 전혀 알려주지 않아 사용자가 뭘 해야 할지 모르는 경우.
5. 카피 — 버튼/링크 문구만으로는 눌렀을 때 뭐가 일어날지 전혀 예측이 안 되는 경우
   (예: "확인"만 있고 뭘 확인하는지 문맥이 전혀 없음).
6. 시각적 위계 — 지금 이 화면에서 뭘 해야 하는지 전혀 짐작이 안 될 정도로 정보가
   뒤섞여 있는 경우.
`;

module.exports = { UIUX_CHECKLIST };
